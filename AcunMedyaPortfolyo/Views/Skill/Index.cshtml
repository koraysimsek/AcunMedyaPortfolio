@model List<AcunMedyaPortfolyo.Models.Skill>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}

<h2>Skill</h2>

<div class="container">
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>SkillID</th>
                <th>Skill Name</th>
                <th>Percentage</th>
                <th>Description</th>
                <th>Update</th>
                <th>Delete</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.SkillID</td>
                    <td>@item.SkillName</td>
                    <td>@item.Percentage</td>
                    <td>@item.Description</td>
                    <td><a href="/Skill/UpdateSkill/@item.SkillID" class="btn btn-success">Update</a></td>
                    <td><a href="/Skill/DeleteSkill/@item.SkillID" class="btn btn-danger">Delete</a></td>
                </tr>
            }
        </tbody>
    </table>
    <a href="/Skill/CreateSkill/" class="btn btn-success">Create Skill</a>
</div>